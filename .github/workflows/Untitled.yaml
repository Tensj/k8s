name: undininable-ture
on:
  push:
    branches:
      - main
jobs:
  build: 
    runs-on: ubuntu-latest
    steps:
      - name: check github environment
        run: hostname
  deploy: 
    runs-on: ten
    steps:
      - name: check github environment
        run: hostname